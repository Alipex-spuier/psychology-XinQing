# psychology_backend
hyh和xyf的后端
```
docker run -itd --name nginx spuieralipex/nginx:v1 /bin/bash
docker cp nginx:/var/www/ ./
docker stop nginx&&sudo docker rm nginx
sudo curl -o- https://gitee.com/boliang2190/psychology-backend/blob/main/install.sh | bash
```
